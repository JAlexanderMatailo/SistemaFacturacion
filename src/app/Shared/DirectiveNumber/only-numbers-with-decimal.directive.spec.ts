import { OnlyNumbersWithDecimalDirective } from './only-numbers-with-decimal.directive';

describe('OnlyNumbersWithDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumbersWithDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
